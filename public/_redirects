# Static file redirects (highest priority)
/sitemap.xml /sitemap.xml 200
/robots.txt /robots.txt 200

# Legacy URL redirects
/homepage / 301
/light-novel-tieng-viet /light-novel-vietsub 301
/doc-light-novel-vietsub /light-novel-vietsub 301
/latest-update /light-novel-vietsub 301

# API routes should not be caught by SPA fallback
/api/* /.netlify/functions/:splat 200

# Clean URL redirects (remove trailing slashes)
/novel/:novelId/ /novel/:novelId 301!
/novel/:novelId/chapter/:chapterId/ /novel/:novelId/chapter/:chapterId 301!

# SPA fallback - handle all other routes with React Router
# This must be LAST to avoid conflicts
/* /index.html 200 